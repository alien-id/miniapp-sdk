name: Contract Package

on:
  push:
    branches:
      - main
      - develop
    paths:
      - "packages/contract/src/**"
      - "packages/contract/scripts/**"
    tags:
      - "contract@*"
  pull_request:
    branches:
      - main
      - develop
    paths:
      - "packages/contract/src/**"
      - "packages/contract/scripts/**"
  release:
    types: [published]
  workflow_dispatch:

jobs:
  publish:
    name: Build and Publish Contract Package
    if: startsWith(github.ref, 'refs/tags/contract@')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          persist-credentials: false
          fetch-depth: 0
          fetch-tags: true

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Validate tag matches package.json version
        working-directory: packages/contract
        run: |
          TAG_NAME=${GITHUB_REF#refs/tags/}
          PACKAGE_VERSION=$(bun -e "const pkg = await Bun.file('package.json').json(); console.log(pkg.version);")
          EXPECTED_TAG="contract@${PACKAGE_VERSION}"
          if [ "$TAG_NAME" != "$EXPECTED_TAG" ]; then
            echo "Error: Tag $TAG_NAME does not match package.json version $PACKAGE_VERSION"
            exit 1
          fi
          echo "✓ Tag matches package.json version"

      - name: Build package
        working-directory: packages/contract
        run: bun run build

      - name: Generate JSON schemas
        working-directory: packages/contract
        run: bun run generate-schemas

      - name: Configure npm for trusted publishing
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'

      - name: Publish to npm
        working-directory: packages/contract
        run: bun publish --access public --provenance

      - name: Upload JSON schema artifacts
        uses: actions/upload-artifact@v4
        with:
          name: contract-schemas
          path: |
            packages/contract/artifacts/events.schema.json
            packages/contract/artifacts/methods.schema.json
          retention-days: 90

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          append_body: true
          body: |
            ## npm
            https://www.npmjs.com/package/@alien_org/contract
          files: |
            packages/contract/artifacts/events.schema.json
            packages/contract/artifacts/methods.schema.json
