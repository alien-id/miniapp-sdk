name: Bridge Package

on:
  push:
    branches:
      - main
      - develop
    paths:
      - "packages/bridge/src/**"
      - "packages/bridge/scripts/**"
    tags:
      - "bridge@*"
  pull_request:
    branches:
      - main
      - develop
    paths:
      - "packages/bridge/src/**"
      - "packages/bridge/scripts/**"
  release:
    types: [published]
  workflow_dispatch:

permissions: {}

jobs:
  publish:
    if: startsWith(github.ref, 'refs/tags/bridge@')
    uses: ./.github/workflows/reusable-publish-npm-package.yml
    permissions:
      contents: write
      id-token: write
    with:
      package-name: bridge
